'use strict';

describe('vaultPortal.status module', function() {

  beforeEach(module('vaultPortal.status'));

  describe('status controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('StatusCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});